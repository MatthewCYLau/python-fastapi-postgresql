apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: python-app-data
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard-rwo
